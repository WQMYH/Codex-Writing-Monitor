[project]
name = "writing-ops-browser-worker"
version = "0.1.0"
description = "Bounded browser-use fallback worker for Writing Ops"
requires-python = ">=3.12,<3.13"
dependencies = ["browser-use==0.13.8"]
